package android.support.v4.widget;

import android.support.v4.view.an;
import android.view.View;

class ae implements ad {
    ae() {
    }

    public void m648a(SlidingPaneLayout slidingPaneLayout, View view) {
        an.m363a(slidingPaneLayout, view.getLeft(), view.getTop(), view.getRight(), view.getBottom());
    }
}
